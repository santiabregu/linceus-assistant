# The config recipe.
# https://rasa.com/docs/rasa/model-configuration/
recipe: default.v1

# The assistant project unique identifier
# This default value must be replaced with a unique assistant name within your deployment
assistant_id: 20250810-180416-resolving-cottage

# Configuration for Rasa NLU.
# https://rasa.com/docs/rasa/nlu/components/
language: es

pipeline:
  # Step 1: Tokenization - Breaking text into words/tokens
  - name: WhitespaceTokenizer
      
  # Step 2: Pattern Recognition - For course codes, emails, etc.
  - name: RegexFeaturizer
  
  # Step 3: Linguistic Features - Grammar and syntax understanding
  - name: LexicalSyntacticFeaturizer
  
  # Step 4: Word-level features - Understanding word meanings
  - name: CountVectorsFeaturizer
    analyzer: word
    min_ngram: 1
    max_ngram: 2
    
  # Step 5: Character-level features - Handling typos and variations
  - name: CountVectorsFeaturizer
    analyzer: char_wb
    min_ngram: 1
    max_ngram: 4
    
  # Step 6: Main Intent Classifier - The brain that decides what users want
  - name: DIETClassifier
    epochs: 150
    constrain_similarities: true
    dropout_rate: 0.2
    
  # Step 7: Entity Synonym Mapping - Handles different names for same thing
  - name: EntitySynonymMapper
  
  # Step 8: Response Selection - For FAQ-style responses
  - name: ResponseSelector
    epochs: 150
    constrain_similarities: true
    
  # Step 9: Fallback Detection - When confidence is too low
  - name: FallbackClassifier
    threshold: 0.6
    ambiguity_threshold: 0.15

# # No configuration for the NLU pipeline was provided. The following default pipeline was used to train your model.
# # If you'd like to customize it, uncomment and adjust the pipeline.
# # See https://rasa.com/docs/rasa/tuning-your-model for more information.
#   - name: WhitespaceTokenizer
#   - name: RegexFeaturizer
#   - name: LexicalSyntacticFeaturizer
#   - name: CountVectorsFeaturizer
#   - name: CountVectorsFeaturizer
#     analyzer: char_wb
#     min_ngram: 1
#     max_ngram: 4
#   - name: DIETClassifier
#     epochs: 100
#     constrain_similarities: true
#   - name: EntitySynonymMapper
#   - name: ResponseSelector
#     epochs: 100
#     constrain_similarities: true
#   - name: FallbackClassifier
#     threshold: 0.3
#     ambiguity_threshold: 0.1

# Configuration for Rasa Core.
# https://rasa.com/docs/rasa/core/policies/

policies:
  - name: MemoizationPolicy
  - name: RulePolicy
    core_fallback_threshold: 0.4
    core_fallback_action_name: "action_default_fallback"
  - name: UnexpecTEDIntentPolicy
    max_history: 5
    epochs: 100
  - name: TEDPolicy
    max_history: 5
    epochs: 100
    constrain_similarities: true
