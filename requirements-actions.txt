rasa-sdk==3.6.2
python-dotenv==1.0.0
supabase==2.3.0
numpy==1.24.4
spacy==3.7.6
rapidfuzz==3.6.1
psycopg2-binary==2.9.9
requests>=2.31.0
# Sólo para embeddings (rag/embeddings.py usa genai.embed_content). La generación
# de texto vive en actions/shared/gemini_client.py llamando al endpoint REST con
# requests, así no nos vemos forzados a actualizar la SDK (que chocaría con httpx
# de supabase y websockets de rasa-sdk).
google-generativeai>=0.7.0
# Rasa 3.6 importa packaging.version.LegacyVersion, eliminado en packaging>=22.0
packaging<22.0
